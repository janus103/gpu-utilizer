Input Image (B, 3, 224, 224)
        │
        ├─────────────────────────────────────┐
        ▼                                     ▼
┌──────────────────────┐           ┌──────────────────────┐
│    AugClassifier     │           │  ViT patch_embed.proj │
│    (사전학습 로드)      │           │  Conv2d(3→768, k=16) │
│                      │           │                      │
│  stage1~4 + proj     │           │  → embedding_feat    │
│  → feat (B,768,14,14)│           │    (B, 768, 14, 14)  │
│  → logits (B, 8)     │           └──────────┬───────────┘
└──────────┬───────────┘                      │
           │ aug_feat                         │ embedding_feat
           │                                  │
           └──────────── + ───────────────────┘
                         │
                         ▼
                combined = aug_feat + embedding_feat
                         (B, 768, 14, 14)
                         │
                         ▼
          ┌──────────────────────────────┐
          │     Parallel Attention       │
          │  (기존 train_fedavg 방식)       │
          │                              │
          │  channel_attn(combined)       │
          │    → attn (B, 768, 1, 1)     │
          │                              │
          │  spatial_attn(combined)       │
          │    → mask (B, 1, 14, 14)     │
          │                              │
          │  output = mask * attn * combined │
          │    → (B, 768, 14, 14)        │
          └──────────────┬───────────────┘
                         │
                         ▼
                flatten → (B, 196, 768)
                patch_embed.norm
                + pos_embed
                         │
                         ▼
          ┌──────────────────────────────┐
          │     ViT Backbone (Frozen)    │
          │     (Parallel Attention 없음)  │
          │                              │
          │  Block 0 → Block 1 → ... → 11│
          │  (vanilla transformer blocks) │
          │                              │
          │  norm → forward_head         │
          │  → class_logits (B, 1000)    │
          └──────────────────────────────┘
